export class Ingredient{
    constructor(public name: string, public amount: number){
            //same as if we would define public variables in our class and then assign them in the constructor
    }
}